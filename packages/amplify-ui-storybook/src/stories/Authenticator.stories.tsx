import React from 'react';
import { AmplifyAuthenticator } from '@aws-amplify/ui-react';

export default {
	title: 'Authenticator',
	component: AmplifyAuthenticator,
};

export const defaults = () => <AmplifyAuthenticator />;
